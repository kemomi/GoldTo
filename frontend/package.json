{
  "name": "goldto-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "GoldTo Frontend — Swarm Intelligence Prediction Engine",
  "scripts": {
    "dev": "npx serve . -p 3000 --no-clipboard",
    "build": "echo 'No build step needed — pure HTML/JS/CSS'",
    "preview": "npx serve . -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
