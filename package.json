{
  "name": "goldto",
  "version": "1.0.0",
  "description": "GoldTo — Swarm Intelligence Prediction Engine",
  "scripts": {
    "dev": "bash dev.sh",
    "setup": "bash setup.sh",
    "setup:all": "bash setup.sh",
    "backend": "cd backend && uvicorn main:app --host 0.0.0.0 --port 5001 --reload",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build"
  }
}
